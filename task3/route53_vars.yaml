---
- name: this creates Route53 (DNS) with your help
  route53:
      state: present
      zone: "{{zone}}"
      record: "{{record}}"
      type: A
      ttl: "{{ttl}}"
      value: "{{value}}"
      wait: yes